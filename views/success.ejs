<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>success</title>
</head>
<body>
  成功
  <input type="hidden" id="url" value="<%= status%>"/>
  <p><span id="times">3</span>s后跳转主页...</p>
  <script src="libs/jquery/jquery-1.10.2.min.js"></script>
<script>

  var itvl = setInterval(function () {
    let times = $("#times").text();
    console.log(times);
    if (times > 1) {
      $("#times").text(--times)
    } else {
      clearInterval(itvl);
      window.location.href = $("#url").val()
    }
  }, 1000);
</script>
</body>
</html>